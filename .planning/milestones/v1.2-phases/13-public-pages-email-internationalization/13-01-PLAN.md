---
phase: 13-public-pages-email-internationalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks.server.ts
  - src/routes/+layout.server.ts
  - src/lib/i18n/en.json
  - src/lib/i18n/zh.json
  - src/routes/+page.svelte
  - src/lib/components/landing/LandingNav.svelte
  - src/lib/components/landing/LandingHero.svelte
  - src/lib/components/landing/FeaturesGrid.svelte
  - src/lib/components/landing/CostComparison.svelte
  - src/lib/components/landing/SelfHostCta.svelte
  - src/lib/components/landing/LandingFooter.svelte
  - src/routes/auth/+layout.svelte
  - src/routes/auth/login/+page.svelte
  - src/routes/auth/signup/+page.svelte
  - src/routes/auth/forgot-password/+page.svelte
  - src/routes/auth/reset-password/+page.svelte
  - src/routes/auth/verify-email/+page.svelte
  - src/routes/auth/invite/[token]/+page.svelte
  - src/routes/auth/oauth/confirm-link/+page.svelte
autonomous: true
requirements:
  - I18N-03

must_haves:
  truths:
    - "Unauthenticated visitor sees landing page in their browser's language (zh-* maps to zh, else en)"
    - "Unauthenticated visitor can manually switch language on public pages via LanguageSwitcher"
    - "Language preference persists in cookie for unauthenticated users across page loads"
    - "All auth pages (login, signup, forgot-password, reset-password, verify-email, invite, oauth confirm) render in selected language"
    - "Cookie preference overrides browser locale detection"
  artifacts:
    - path: "src/hooks.server.ts"
      provides: "Accept-Language detection + cookie reading for unauthenticated users"
      contains: "accept-language"
    - path: "src/routes/+layout.server.ts"
      provides: "Locale passed for both authenticated and unauthenticated users"
      contains: "locale"
    - path: "src/lib/i18n/en.json"
      provides: "English translations for landing + auth pages"
      contains: "landing"
    - path: "src/lib/i18n/zh.json"
      provides: "Chinese translations for landing + auth pages"
      contains: "landing"
  key_links:
    - from: "src/hooks.server.ts"
      to: "event.locals"
      via: "Accept-Language header parsing + cookie fallback"
      pattern: "accept-language|lang.*cookie"
    - from: "src/routes/+layout.server.ts"
      to: "src/routes/+layout.svelte"
      via: "locale passed in load return"
      pattern: "locale"
    - from: "src/routes/auth/login/+page.svelte"
      to: "src/lib/i18n/en.json"
      via: "$t() calls"
      pattern: "\\$t\\("
---

<objective>
Add browser locale detection for unauthenticated users and translate all public pages (landing + auth) to Chinese/English.

Purpose: Unauthenticated visitors experience the product in their preferred language, fulfilling I18N-03.
Output: Locale-aware public pages with LanguageSwitcher, cookie persistence, and complete en/zh translations for landing and auth content.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-public-pages-email-internationalization/13-CONTEXT.md

<interfaces>
<!-- Existing i18n infrastructure from Phase 12 -->

From src/lib/i18n/index.ts:
- svelte-i18n initialized with addMessages('en', en) and addMessages('zh', zh)
- init({ fallbackLocale: 'en', initialLocale: 'en' })

From src/hooks.server.ts:
- resolveWithLang helper uses event.locals.user?.language ?? 'en' for HTML lang attribute
- Must be extended to support cookie/Accept-Language fallback for unauthenticated users

From src/routes/+layout.server.ts:
- Returns { user, locale: event.locals.user?.language ?? 'en' }
- Must be extended to check cookie/Accept-Language when user is null

From src/lib/components/layout/LanguageSwitcher.svelte:
- Existing component that POSTs to API to update DB preference
- Must be extended/reused on public pages with cookie fallback for unauthenticated users
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser locale detection, cookie persistence, and public page locale plumbing</name>
  <files>src/hooks.server.ts, src/routes/+layout.server.ts, src/routes/auth/+layout.svelte</files>
  <read_first>
    - src/hooks.server.ts
    - src/routes/+layout.server.ts
    - src/routes/+layout.svelte
    - src/lib/components/layout/LanguageSwitcher.svelte
    - src/routes/auth/+layout.svelte
  </read_first>
  <action>
1. In `src/hooks.server.ts`:
   - Add a helper function `detectLocale(event)` that returns locale string:
     a. If `event.locals.user?.language` exists, return it (authenticated user)
     b. Else check `event.cookies.get('lang')` — if 'zh' return 'zh', if 'en' return 'en'
     c. Else parse `event.request.headers.get('accept-language')` — if any `zh` variant found (zh, zh-CN, zh-TW, zh-HK), return 'zh'
     d. Else return 'en'
   - Store result on `event.locals.locale` (add `locale: string` to App.Locals type in app.d.ts if needed)
   - Update `resolveWithLang` to use `e.locals.locale ?? 'en'` instead of `e.locals.user?.language ?? 'en'`
   - Set `event.locals.locale` early in the handle function (before any resolve call), for ALL paths (v1, no-token, authenticated)

2. In `src/routes/+layout.server.ts`:
   - Change locale return to: `locale: event.locals.locale ?? event.locals.user?.language ?? 'en'`

3. In `src/routes/auth/+layout.svelte` (auth layout):
   - Import LanguageSwitcher component
   - Add it to the auth layout so it appears on all auth pages (top-right corner, small)
   - For unauthenticated users, LanguageSwitcher should set a cookie `lang` (value 'en' or 'zh', path '/', maxAge 1 year, sameSite 'lax') instead of POSTing to the API
   - If LanguageSwitcher needs modification to support cookie mode, update it to accept an `authenticated` prop (default true). When `authenticated=false`, set cookie + reload page instead of API call.

4. On landing page `src/routes/+page.svelte`:
   - Import LanguageSwitcher
   - The LandingNav component should include LanguageSwitcher. Read LandingNav.svelte and add `<LanguageSwitcher authenticated={false} />` to the nav bar.

5. Update `src/app.d.ts` to add `locale?: string` to `App.Locals` interface if not already present.
  </action>
  <verify>
    <automated>cd /Users/hzmhezhiming/projects/opensource-projects/OpenRouterAround/llmtokenhub && grep -n "accept-language\|detectLocale\|lang.*cookie\|locals.locale" src/hooks.server.ts && grep -n "locale" src/routes/+layout.server.ts && grep -n "LanguageSwitcher" src/routes/auth/+layout.svelte src/lib/components/landing/LandingNav.svelte</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks.server.ts contains Accept-Language header parsing with zh-* variant detection
    - src/hooks.server.ts reads 'lang' cookie for unauthenticated users
    - event.locals.locale is set for all request paths
    - resolveWithLang uses locals.locale
    - +layout.server.ts returns locale from event.locals.locale
    - LanguageSwitcher appears in auth layout and LandingNav
    - LanguageSwitcher supports unauthenticated mode (cookie-based)
  </acceptance_criteria>
  <done>Unauthenticated users get locale from Accept-Language or cookie; LanguageSwitcher visible on all public pages; locale flows to client for svelte-i18n</done>
</task>

<task type="auto">
  <name>Task 2: Translate landing page and all auth pages with $t() wiring</name>
  <files>src/lib/i18n/en.json, src/lib/i18n/zh.json, src/routes/+page.svelte, src/lib/components/landing/LandingHero.svelte, src/lib/components/landing/FeaturesGrid.svelte, src/lib/components/landing/CostComparison.svelte, src/lib/components/landing/SelfHostCta.svelte, src/lib/components/landing/LandingFooter.svelte, src/routes/auth/login/+page.svelte, src/routes/auth/signup/+page.svelte, src/routes/auth/forgot-password/+page.svelte, src/routes/auth/reset-password/+page.svelte, src/routes/auth/verify-email/+page.svelte, src/routes/auth/invite/[token]/+page.svelte, src/routes/auth/oauth/confirm-link/+page.svelte</files>
  <read_first>
    - src/lib/i18n/en.json
    - src/lib/i18n/zh.json
    - src/lib/components/landing/LandingHero.svelte
    - src/lib/components/landing/FeaturesGrid.svelte
    - src/lib/components/landing/CostComparison.svelte
    - src/lib/components/landing/SelfHostCta.svelte
    - src/lib/components/landing/LandingFooter.svelte
    - src/lib/components/landing/LandingNav.svelte
    - src/routes/auth/login/+page.svelte
    - src/routes/auth/signup/+page.svelte
    - src/routes/auth/forgot-password/+page.svelte
    - src/routes/auth/reset-password/+page.svelte
    - src/routes/auth/verify-email/+page.svelte
    - src/routes/auth/invite/[token]/+page.svelte
    - src/routes/auth/oauth/confirm-link/+page.svelte
    - src/routes/+page.svelte
  </read_first>
  <action>
1. Add translation keys to `en.json` and `zh.json` (EXTEND existing files, do NOT replace). Use flat dot-notation keys following Phase 12 conventions. Add these key groups:

   **Landing page keys** (prefix `landing.`):
   - `landing.title`: "StarAIGateway" / "StarAIGateway"
   - `landing.subtitle`: exact text from LandingHero ("Share AI access with your team. Save 40-60% on LLM costs.") / Chinese equivalent
   - `landing.description`: the longer paragraph from LandingHero / Chinese equivalent
   - `landing.cta`: "Get Started Free" / "免费开始"
   - `landing.meta_title`: SEO title / Chinese SEO title
   - `landing.meta_description`: SEO description / Chinese
   - All text from FeaturesGrid (feature titles, descriptions — read file for exact strings)
   - All text from CostComparison (headers, labels, values — read file for exact strings)
   - All text from SelfHostCta (heading, description, button text)
   - All text from LandingFooter (links, copyright, tagline)
   - All text from LandingNav (nav links)

   **Auth page keys** (prefix `auth.`):
   - `auth.login.title`: "Log in to your account" / "登录您的账户"
   - `auth.login.submit`: "Log in" / "登录"
   - `auth.login.loading`: "Logging in..." / "登录中..."
   - `auth.login.no_account`: "Don't have an account?" / "还没有账户？"
   - `auth.login.signup_link`: "Sign up" / "注册"
   - `auth.login.forgot_password`: "Forgot password?" / "忘记密码？"
   - `auth.login.oauth_error`: "Sign in failed. Please try again." / "登录失败，请重试。"
   - `auth.login.page_title`: "Log In - StarAIGateway" / "登录 - StarAIGateway"
   - Similarly for signup, forgot-password, reset-password, verify-email, invite, oauth confirm-link — read each file for exact hardcoded strings
   - `auth.email_label`: "Email" / "邮箱"
   - `auth.password_label`: "Password" / "密码"
   - `auth.name_label`: "Name" / "姓名"

2. Wire `$t()` into ALL landing components:
   - Add `import { t } from 'svelte-i18n';` at top of each `<script>` block (or use `import { t } from '$lib/i18n';` if that's the pattern — check existing dashboard components)
   - Replace every hardcoded English string with `{$t('landing.key_name')}`
   - For `<svelte:head><title>`, use `{$t('landing.meta_title')}`
   - LandingHero: replace h1 text, subtitle p, description p, CTA button text
   - FeaturesGrid: replace all feature titles and descriptions
   - CostComparison: replace all headers, labels, comparison text
   - SelfHostCta: replace heading, description, button
   - LandingFooter: replace all text
   - LandingNav: replace nav link text

3. Wire `$t()` into ALL auth pages:
   - `login/+page.svelte`: Replace "Log in to your account", "Email", "Password", "Forgot password?", button text, "Don't have an account?", "Sign up", oauth error message, page title
   - `signup/+page.svelte`: Replace all hardcoded strings (read file first for exact content)
   - `forgot-password/+page.svelte`: Replace all hardcoded strings
   - `reset-password/+page.svelte`: Replace all hardcoded strings
   - `verify-email/+page.svelte`: Replace all hardcoded strings
   - `invite/[token]/+page.svelte`: Replace all hardcoded strings
   - `oauth/confirm-link/+page.svelte`: Replace all hardcoded strings

4. Use `{$t('common.email')}` for shared labels already in en.json (Email, Password, etc.) — check existing keys first to avoid duplication.
  </action>
  <verify>
    <automated>cd /Users/hzmhezhiming/projects/opensource-projects/OpenRouterAround/llmtokenhub && echo "=== en.json landing keys ===" && node -e "const j=require('./src/lib/i18n/en.json'); console.log(Object.keys(j.landing || {}).length, 'landing keys'); console.log(Object.keys(j.auth || {}).length, 'auth keys')" && echo "=== $t usage in landing ===" && grep -c '\$t(' src/lib/components/landing/*.svelte && echo "=== $t usage in auth ===" && grep -c '\$t(' src/routes/auth/*/+page.svelte src/routes/auth/invite/*/+page.svelte src/routes/auth/oauth/confirm-link/+page.svelte 2>/dev/null && echo "=== No hardcoded UI strings in auth pages ===" && grep -cP '>[A-Z][a-z]+.*</' src/routes/auth/login/+page.svelte || true</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains `landing.*` keys covering all landing page text
    - en.json contains `auth.*` keys covering all auth page text
    - zh.json contains matching Chinese translations for all new keys
    - Every landing component uses $t() — no hardcoded English strings remain
    - Every auth page uses $t() — no hardcoded English strings remain
    - Existing keys in en.json/zh.json are preserved (not overwritten)
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>All public pages (landing + 7 auth pages) render text via $t() with complete en/zh translations; no hardcoded English strings remain in public-facing components</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Landing page renders correctly with all text from translation keys
3. Auth pages render correctly with translated text
4. Browser with `Accept-Language: zh-CN` sees Chinese on landing page
5. Cookie `lang=zh` overrides browser locale
6. LanguageSwitcher visible and functional on public pages
</verification>

<success_criteria>
- Unauthenticated users see public pages in their browser's language
- Manual language switch works on public pages via cookie persistence
- All landing and auth page text comes from i18n translation files
- Both en.json and zh.json have complete translations for all public page content
</success_criteria>

<output>
After completion, create `.planning/phases/13-public-pages-email-internationalization/13-01-SUMMARY.md`
</output>
