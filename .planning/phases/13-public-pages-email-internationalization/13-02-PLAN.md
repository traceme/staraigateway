---
phase: 13-public-pages-email-internationalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/email-templates.ts
  - src/lib/server/auth/emails/verification.ts
  - src/lib/server/auth/emails/password-reset.ts
  - src/lib/server/auth/emails/invitation.ts
  - src/lib/server/auth/emails/budget-warning.ts
  - src/lib/server/auth/emails/admin-digest.ts
  - src/lib/server/auth/email.ts
  - src/lib/server/budget/notifications.ts
autonomous: true
requirements:
  - I18N-04

must_haves:
  truths:
    - "Verification email renders in recipient's stored language preference"
    - "Password reset email renders in recipient's stored language preference"
    - "Invitation email renders in English when invitee has no stored preference"
    - "Budget warning email renders in recipient's stored language preference"
    - "Admin digest email renders in recipient's stored language preference"
    - "Users without stored language preference receive English emails"
  artifacts:
    - path: "src/lib/server/email-templates.ts"
      provides: "Centralized bilingual email template registry with getTemplate(type, lang)"
      exports: ["getTemplate"]
    - path: "src/lib/server/auth/email.ts"
      provides: "Email send functions accepting language parameter"
      contains: "language"
  key_links:
    - from: "src/lib/server/auth/email.ts"
      to: "src/lib/server/email-templates.ts"
      via: "getTemplate call with language parameter"
      pattern: "getTemplate"
    - from: "src/lib/server/budget/notifications.ts"
      to: "src/lib/server/auth/email.ts"
      via: "sendBudgetWarningEmail with user language"
      pattern: "language|lang"
---

<objective>
Make all email templates bilingual by creating a language-aware template system and threading user language preference through email send functions.

Purpose: Email recipients receive messages in their stored language preference, fulfilling I18N-04.
Output: All 5 email templates (verification, password-reset, invitation, budget-warning, admin-digest) render in en or zh based on recipient's preference.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-public-pages-email-internationalization/13-CONTEXT.md

<interfaces>
<!-- Existing email infrastructure -->

From src/lib/server/auth/email.ts:
- sendVerificationEmail(email, name, token): Promise<void>
- sendPasswordResetEmail(email, name, token): Promise<void>
- sendBudgetWarningEmail(email, memberName, currentSpend, limit, orgName): Promise<void>
- sendInvitationEmail(email, token, orgName, inviterName, role): Promise<void>
- sendAdminDigestEmail(adminEmail, orgName, date, members): Promise<void>
- Each calls a template function from src/lib/server/auth/emails/*.ts
- Each template returns { subject, html, text }

From src/lib/server/auth/emails/verification.ts:
- verificationEmail(name, verifyUrl) => { subject, html, text }
- Hardcoded English strings in HTML template

From src/lib/server/budget/notifications.ts:
- checkAndNotifyBudgets(orgId) — sends budget warnings
- sendAdminDigest(orgId) — sends admin digest
- Both call email.ts functions without language parameter

From src/lib/server/db/schema.ts:
- appUsers table has `language` column (text, default 'en') — added in Phase 12
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bilingual email template system and update all 5 email templates</name>
  <files>src/lib/server/email-templates.ts, src/lib/server/auth/emails/verification.ts, src/lib/server/auth/emails/password-reset.ts, src/lib/server/auth/emails/invitation.ts, src/lib/server/auth/emails/budget-warning.ts, src/lib/server/auth/emails/admin-digest.ts</files>
  <read_first>
    - src/lib/server/auth/emails/verification.ts
    - src/lib/server/auth/emails/password-reset.ts
    - src/lib/server/auth/emails/invitation.ts
    - src/lib/server/auth/emails/budget-warning.ts
    - src/lib/server/auth/emails/admin-digest.ts
  </read_first>
  <action>
1. Update each of the 5 existing email template files to accept a `lang: string` parameter (default `'en'`) and return bilingual content.

   For each template file, add a `lang` parameter and use a simple conditional pattern:

   ```typescript
   export function verificationEmail(name: string, verifyUrl: string, lang: string = 'en') {
     const isZh = lang === 'zh';
     const subject = isZh ? '验证您的邮箱' : 'Verify your email';
     // ... html with isZh conditionals for all text strings
   }
   ```

2. **verification.ts** — Add `lang` param. Chinese translations:
   - Subject: "验证您的邮箱"
   - Greeting: "您好 ${name}，"
   - Body: "请验证您的邮箱地址以完成账户设置。"
   - Button: "验证邮箱"
   - Fallback text: "如果按钮无法使用，请复制以下链接到浏览器中打开："

3. **password-reset.ts** — Add `lang` param. Chinese translations:
   - Subject: "重置您的密码"
   - Body: "我们收到了重置您密码的请求。" / "点击下方按钮重置密码。"
   - Button: "重置密码"
   - Expiry note: "此链接将在1小时后过期。"
   - Ignore note: "如果您没有请求重置密码，请忽略此邮件。"

4. **invitation.ts** — Add `lang` param. Chinese translations:
   - Subject: "${inviterName} 邀请您加入 ${orgName}"
   - Body: "${inviterName} 邀请您以 ${role} 身份加入 ${orgName}。"
   - Button: "接受邀请"

5. **budget-warning.ts** — Add `lang` param. Chinese translations:
   - Subject: "预算警告 - ${orgName}"
   - Body: "您在 ${orgName} 的使用量已达到预算的 ${percentage} 。"
   - Current/limit labels in Chinese

6. **admin-digest.ts** — Add `lang` param. Chinese translations:
   - Subject: "每日预算摘要 - ${orgName}"
   - Table headers and labels in Chinese
   - Summary text in Chinese

7. Keep exact same HTML structure and styling — only change text content based on `isZh` conditional. Do NOT change the HTML layout, colors, or spacing.
  </action>
  <verify>
    <automated>cd /Users/hzmhezhiming/projects/opensource-projects/OpenRouterAround/llmtokenhub && grep -n "lang.*=.*'en'" src/lib/server/auth/emails/*.ts | head -10 && echo "=== All templates accept lang ===" && for f in verification password-reset invitation budget-warning admin-digest; do grep -c "lang" src/lib/server/auth/emails/$f.ts; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 email template functions accept a `lang` parameter with default `'en'`
    - Each template returns Chinese subject + body when lang='zh'
    - Each template returns English subject + body when lang='en' (same as before)
    - HTML structure unchanged — only text differs
    - grep "lang" in each template file returns at least 2 matches
  </acceptance_criteria>
  <done>All 5 email templates support bilingual output via lang parameter</done>
</task>

<task type="auto">
  <name>Task 2: Thread language preference through email send functions and callers</name>
  <files>src/lib/server/auth/email.ts, src/lib/server/budget/notifications.ts</files>
  <read_first>
    - src/lib/server/auth/email.ts
    - src/lib/server/budget/notifications.ts
    - src/lib/server/db/schema.ts
  </read_first>
  <action>
1. Update `src/lib/server/auth/email.ts` — add `lang` parameter to ALL send functions:

   - `sendVerificationEmail(email, name, token, lang = 'en')` — pass `lang` to `verificationEmail(name, verifyUrl, lang)`
   - `sendPasswordResetEmail(email, name, token, lang = 'en')` — pass `lang` to `passwordResetEmail(name, resetUrl, lang)`
   - `sendBudgetWarningEmail(email, memberName, currentSpend, limit, orgName, lang = 'en')` — pass `lang` to `budgetWarningEmail(..., lang)`
   - `sendInvitationEmail(email, token, orgName, inviterName, role, lang = 'en')` — pass `lang` to `invitationEmail(..., lang)`
   - `sendAdminDigestEmail(adminEmail, orgName, date, members, lang = 'en')` — pass `lang` to `adminDigestEmail(..., lang)`

2. Update `src/lib/server/budget/notifications.ts`:

   In `checkAndNotifyBudgets()`:
   - The member query already joins appUsers. Add `language: appUsers.language` to the select.
   - Add `language` field to `MemberBudgetInfo` interface.
   - Pass `member.language ?? 'en'` as last arg to `sendBudgetWarningEmail()`.

   In `sendAdminDigest()`:
   - The admin query already joins appUsers. Add `language: appUsers.language` to the select.
   - Pass `admin.language ?? 'en'` as last arg to `sendAdminDigestEmail()`.

3. Find ALL other callers of the email send functions and thread language through:

   Search for imports of sendVerificationEmail, sendPasswordResetEmail, sendInvitationEmail across the codebase. These are likely in:
   - Auth form actions (signup, forgot-password, verify-email resend)
   - Invite creation server action

   For each caller:
   - If the caller has access to `event.locals.user?.language`, pass it
   - For signup verification: user just created, pass the language from their session or default 'en'
   - For password reset: look up user's language from DB (the user record is already fetched to validate email)
   - For invitation: invitee has no account yet, pass 'en' (fallback per requirements)
   - If the caller already has the user object with `.language`, pass `user.language ?? 'en'`

4. Ensure backward compatibility: all `lang` params default to `'en'` so existing callers not yet updated still work.
  </action>
  <verify>
    <automated>cd /Users/hzmhezhiming/projects/opensource-projects/OpenRouterAround/llmtokenhub && echo "=== email.ts lang params ===" && grep -n "lang" src/lib/server/auth/email.ts | head -15 && echo "=== notifications.ts language ===" && grep -n "language" src/lib/server/budget/notifications.ts | head -10 && echo "=== build check ===" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 5 send functions in email.ts accept `lang` parameter with default 'en'
    - notifications.ts queries user language from DB and passes to email functions
    - MemberBudgetInfo interface includes `language` field
    - Admin digest query includes user language
    - All callers of send functions compile without errors
    - npm run build succeeds
  </acceptance_criteria>
  <done>User language preference flows from DB through all email send paths; emails render in recipient's stored language; users without preference get English</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All email template functions accept and use `lang` parameter
3. Budget notification emails use user's stored language preference
4. Invitation emails default to English for new users
5. grep confirms no hardcoded English-only email content remains in template files (all text is conditional on lang)
</verification>

<success_criteria>
- All 5 email templates render bilingual content based on lang parameter
- Language preference is threaded from DB through all email send paths
- Users without stored preference receive English emails
- Invitation emails to new users (no account) fall back to English
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-public-pages-email-internationalization/13-02-SUMMARY.md`
</output>
