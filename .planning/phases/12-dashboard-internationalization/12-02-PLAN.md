---
phase: 12-dashboard-internationalization
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  # Layout components
  - src/lib/components/layout/Sidebar.svelte
  - src/lib/components/layout/OrgSwitcher.svelte
  # Dashboard components
  - src/lib/components/dashboard/OnboardingChecklist.svelte
  - src/lib/components/dashboard/AdminKpiCards.svelte
  # API key components
  - src/lib/components/api-keys/CreateKeyModal.svelte
  - src/lib/components/api-keys/KeyCreatedModal.svelte
  - src/lib/components/api-keys/RateLimitFields.svelte
  - src/lib/components/api-keys/SmartRoutingToggle.svelte
  # Budget components
  - src/lib/components/budget/BudgetBanner.svelte
  - src/lib/components/budget/BudgetDefaultsForm.svelte
  - src/lib/components/budget/BudgetPanel.svelte
  # Member components
  - src/lib/components/members/InvitePanel.svelte
  - src/lib/components/members/MemberActionMenu.svelte
  - src/lib/components/members/MembersTable.svelte
  - src/lib/components/members/RoleBadge.svelte
  # Model components
  - src/lib/components/models/ModelPricingTable.svelte
  # Settings components
  - src/lib/components/settings/CacheTtlSetting.svelte
  - src/lib/components/settings/OrgSettingsForm.svelte
  - src/lib/components/settings/SmartRoutingSettings.svelte
  # Usage components
  - src/lib/components/usage/TimeRangePicker.svelte
  - src/lib/components/usage/CostTrendChart.svelte
  - src/lib/components/usage/UsageTable.svelte
  - src/lib/components/usage/BreakdownBarChart.svelte
  - src/lib/components/usage/UsageTabs.svelte
  - src/lib/components/usage/KpiCard.svelte
  # Docs components
  - src/lib/components/docs/IntegrationGuide.svelte
  - src/lib/components/docs/ToolTabs.svelte
  # Page routes
  - src/routes/org/[slug]/dashboard/+page.svelte
  - src/routes/org/[slug]/api-keys/+page.svelte
  - src/routes/org/[slug]/members/+page.svelte
  - src/routes/org/[slug]/provider-keys/+page.svelte
  - src/routes/org/[slug]/usage/+page.svelte
  - src/routes/org/[slug]/settings/+page.svelte
  - src/routes/org/[slug]/models/+page.svelte
  - src/routes/org/create/+page.svelte
  - src/routes/org/[slug]/+layout.svelte
  # Server action files (error key migration)
  - src/routes/org/[slug]/api-keys/+page.server.ts
  - src/routes/org/[slug]/members/+page.server.ts
  - src/routes/org/[slug]/provider-keys/+page.server.ts
  - src/routes/org/[slug]/settings/+page.server.ts
  - src/routes/org/[slug]/usage/budget/+server.ts
  - src/routes/org/create/+page.server.ts
autonomous: true
requirements:
  - I18N-02
  - I18N-05
must_haves:
  truths:
    - "All dashboard page headings, button labels, table headers, and descriptions render in the user's selected language"
    - "Sidebar navigation labels render in the user's selected language"
    - "Empty states and placeholder text render in the user's selected language"
    - "Form validation error messages from server actions display in the user's selected language"
    - "Confirm dialogs (revoke key, remove member) display in the user's selected language"
    - "Dates and numbers format according to the user's locale"
  artifacts:
    - path: "src/lib/components/layout/Sidebar.svelte"
      provides: "Translated navigation labels"
      contains: "$t('nav."
    - path: "src/routes/org/[slug]/api-keys/+page.svelte"
      provides: "Translated API keys page"
      contains: "$t('api_keys."
    - path: "src/routes/org/[slug]/members/+page.svelte"
      provides: "Translated members page"
      contains: "$t('members."
    - path: "src/routes/org/[slug]/api-keys/+page.server.ts"
      provides: "Error keys instead of hardcoded English strings"
      contains: "errorKey"
    - path: "src/routes/org/[slug]/members/+page.server.ts"
      provides: "Error keys instead of hardcoded English strings"
      contains: "errorKey"
  key_links:
    - from: "src/lib/components/layout/Sidebar.svelte"
      to: "src/lib/i18n/en.json"
      via: "$t() store lookups against nav.* keys"
      pattern: "\\$t\\('nav\\."
    - from: "src/routes/org/[slug]/api-keys/+page.svelte"
      to: "src/lib/i18n/en.json"
      via: "$t() store lookups against api_keys.* keys"
      pattern: "\\$t\\('api_keys\\."
    - from: "src/routes/org/[slug]/api-keys/+page.server.ts"
      to: "src/routes/org/[slug]/api-keys/+page.svelte"
      via: "Server returns errorKey, client translates via $t(form.errorKey)"
      pattern: "errorKey.*errors\\."
    - from: "All dashboard components"
      to: "svelte-i18n locale store"
      via: "import { t } from 'svelte-i18n' and $t() calls"
      pattern: "import.*\\{ t \\}.*svelte-i18n"
---

<objective>
Wire all dashboard components and page routes to use svelte-i18n `$t()` for every user-facing string, and migrate server action error messages from hardcoded English strings to translatable error keys.

Purpose: This plan fulfills I18N-02 (all dashboard pages render in selected language) and I18N-05 (error messages display in selected language). Plan 01 created the i18n infrastructure and translation JSON files with all keys. This plan wires every component and page to USE those keys via `$t()` calls.
Output: Every dashboard component and page route uses `$t()` for all user-facing text. Server actions return `errorKey` fields that clients translate via `$t()`. Dates and numbers use locale-aware formatting.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-internationalization/12-CONTEXT.md
@.planning/phases/12-dashboard-internationalization/12-RESEARCH.md
@.planning/phases/12-dashboard-internationalization/12-UI-SPEC.md
@.planning/phases/12-dashboard-internationalization/12-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these — Plan 02 consumes them -->

From src/lib/i18n/index.ts (created by Plan 01):
```typescript
import { addMessages, init } from 'svelte-i18n';
import en from './en.json';
import zh from './zh.json';
addMessages('en', en);
addMessages('zh', zh);
init({ fallbackLocale: 'en', initialLocale: 'en' });
```

Usage pattern in any .svelte file:
```svelte
<script>
  import { t } from 'svelte-i18n';
</script>
<h1>{$t('dashboard.title')}</h1>
```

For locale-aware formatting (dates/numbers), use $locale from svelte-i18n:
```svelte
<script>
  import { t, locale } from 'svelte-i18n';
  // Use $locale in Intl.DateTimeFormat / Intl.NumberFormat
</script>
```

Translation key namespaces in en.json/zh.json (created by Plan 01):
- common.* — save, cancel, delete, create, edit, close, loading, never, active, revoked, confirm, log_out, back, submit, copy, copied, enabled, disabled, none, no_data, search, filter, actions, status, name, email, role, date, description, required
- nav.* — dashboard, provider_keys, api_keys, members, usage, models, settings
- org_switcher.* — create_new, switch_org
- dashboard.* — title, manage_members, view_api_keys, budget_settings, empty_message, welcome
- api_keys.* — title, description, create, my_keys, all_keys, no_keys, no_keys_hint, no_org_keys, table.name/key/status/created/last_used/owner/rpm/tpm, revoke, revoking, revoke_confirm, edit_rate_limits, smart_routing, key_created.title/description/warning, rate_limits.rpm/tpm/rpm_help/tpm_help
- kpi.* — members, active_keys, spend_this_month, requests_this_month
- onboarding.* — get_started, add_provider_key, invite_members, create_api_key
- members.* — title, invite, invite_email, invite_role, invite_submit, invite_sending, no_members, pending_invitations, no_invitations, revoke_invite, remove_member, change_role, remove_confirm
- provider_keys.* — title, description, add_key, no_keys, no_keys_hint, provider, label, api_key, base_url, base_url_hint, models_discovered, remove, remove_confirm, active, inactive, providers.openai/anthropic/google/deepseek/openrouter/custom
- usage.* — title, description, no_data, time_range.7d/30d/90d, tabs.overview/by_model/by_user/by_key, table.model/provider/requests/input_tokens/output_tokens/cost/user/key, cost_trend.title, breakdown.title
- budget.* — title, description, hard_limit, soft_limit, reset_day, no_budget, set_budget, warning_banner, exceeded_banner, defaults.title/description/org_default/role_default, spend_label, of_limit
- models.* — title, description, no_models, table.name/provider/pricing/input_price/output_price, per_million_tokens
- settings.* — title, org_name, org_description, org_slug, save_settings, cache_ttl.title/description/seconds, smart_routing.title/description/cheap_model/expensive_model, danger_zone
- org_create.* — title, name_label, name_placeholder, description_label, description_placeholder, submit, creating
- validation.* — name_required, name_too_long, email_invalid, key_id_required, invalid_role, password_required, password_min_length, slug_invalid
- errors.* — not_authenticated, org_not_found, key_not_found, key_not_in_org, create_key_failed, only_admins_invite, only_admins_remove, only_admins_revoke_invite, only_owner_change_role, only_admins_revoke_keys, only_admins_rate_limits, invite_failed, role_change_failed, remove_failed, revoke_invite_failed, provider_key_failed, settings_failed, budget_failed, generic
- language.* — switch, en, zh
- docs.* — integration_guide.title/description, tool_tabs.cursor/continue/claude_code/openai_sdk

Server action error pattern (I18N-05):
Current: `return fail(400, { error: 'Only admins can invite members' })`
Target:  `return fail(400, { errorKey: 'errors.only_admins_invite' })`
Client:  `{#if form?.errorKey}<p class="text-red-400">{$t(form.errorKey)}</p>{/if}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire $t() into all dashboard components (28 component files)</name>
  <files>
    src/lib/components/layout/Sidebar.svelte,
    src/lib/components/layout/OrgSwitcher.svelte,
    src/lib/components/dashboard/OnboardingChecklist.svelte,
    src/lib/components/dashboard/AdminKpiCards.svelte,
    src/lib/components/api-keys/CreateKeyModal.svelte,
    src/lib/components/api-keys/KeyCreatedModal.svelte,
    src/lib/components/api-keys/RateLimitFields.svelte,
    src/lib/components/api-keys/SmartRoutingToggle.svelte,
    src/lib/components/budget/BudgetBanner.svelte,
    src/lib/components/budget/BudgetDefaultsForm.svelte,
    src/lib/components/budget/BudgetPanel.svelte,
    src/lib/components/members/InvitePanel.svelte,
    src/lib/components/members/MemberActionMenu.svelte,
    src/lib/components/members/MembersTable.svelte,
    src/lib/components/members/RoleBadge.svelte,
    src/lib/components/models/ModelPricingTable.svelte,
    src/lib/components/settings/CacheTtlSetting.svelte,
    src/lib/components/settings/OrgSettingsForm.svelte,
    src/lib/components/settings/SmartRoutingSettings.svelte,
    src/lib/components/usage/TimeRangePicker.svelte,
    src/lib/components/usage/CostTrendChart.svelte,
    src/lib/components/usage/UsageTable.svelte,
    src/lib/components/usage/BreakdownBarChart.svelte,
    src/lib/components/usage/UsageTabs.svelte,
    src/lib/components/usage/KpiCard.svelte,
    src/lib/components/docs/IntegrationGuide.svelte,
    src/lib/components/docs/ToolTabs.svelte
  </files>
  <read_first>
    src/lib/i18n/en.json,
    src/lib/i18n/index.ts,
    src/lib/components/layout/Sidebar.svelte,
    src/lib/components/layout/OrgSwitcher.svelte,
    src/lib/components/dashboard/OnboardingChecklist.svelte,
    src/lib/components/dashboard/AdminKpiCards.svelte,
    src/lib/components/api-keys/CreateKeyModal.svelte,
    src/lib/components/api-keys/KeyCreatedModal.svelte,
    src/lib/components/api-keys/RateLimitFields.svelte,
    src/lib/components/api-keys/SmartRoutingToggle.svelte,
    src/lib/components/budget/BudgetBanner.svelte,
    src/lib/components/budget/BudgetDefaultsForm.svelte,
    src/lib/components/budget/BudgetPanel.svelte,
    src/lib/components/members/InvitePanel.svelte,
    src/lib/components/members/MemberActionMenu.svelte,
    src/lib/components/members/MembersTable.svelte,
    src/lib/components/members/RoleBadge.svelte,
    src/lib/components/models/ModelPricingTable.svelte,
    src/lib/components/settings/CacheTtlSetting.svelte,
    src/lib/components/settings/OrgSettingsForm.svelte,
    src/lib/components/settings/SmartRoutingSettings.svelte,
    src/lib/components/usage/TimeRangePicker.svelte,
    src/lib/components/usage/CostTrendChart.svelte,
    src/lib/components/usage/UsageTable.svelte,
    src/lib/components/usage/BreakdownBarChart.svelte,
    src/lib/components/usage/UsageTabs.svelte,
    src/lib/components/usage/KpiCard.svelte,
    src/lib/components/docs/IntegrationGuide.svelte,
    src/lib/components/docs/ToolTabs.svelte
  </read_first>
  <action>
For each of the 28 component files, apply this transformation pattern:

1. Add `import { t } from 'svelte-i18n';` to the `<script>` block (if the file also needs locale-aware date/number formatting, also import `locale`: `import { t, locale } from 'svelte-i18n';`)

2. Replace every hardcoded English string with the corresponding `$t('key')` call. Match strings to keys using the en.json namespace structure from Plan 01. The key mapping is:

**Layout components:**
- `Sidebar.svelte`: Nav labels — "Dashboard" -> `$t('nav.dashboard')`, "Provider Keys" -> `$t('nav.provider_keys')`, "API Keys" -> `$t('nav.api_keys')`, "Members" -> `$t('nav.members')`, "Usage" -> `$t('nav.usage')`, "Models" -> `$t('nav.models')`, "Settings" -> `$t('nav.settings')`
- `OrgSwitcher.svelte`: "Create new organization" -> `$t('org_switcher.create_new')`

**Dashboard components:**
- `OnboardingChecklist.svelte`: "Get Started" -> `$t('onboarding.get_started')`, "Add your first LLM provider key" -> `$t('onboarding.add_provider_key')`, "Invite team members" -> `$t('onboarding.invite_members')`, "Create an API key" -> `$t('onboarding.create_api_key')`
- `AdminKpiCards.svelte`: "Members" -> `$t('kpi.members')`, "Active Keys" -> `$t('kpi.active_keys')`, "Spend This Month" -> `$t('kpi.spend_this_month')`, "Requests This Month" -> `$t('kpi.requests_this_month')`

**API key components:**
- `CreateKeyModal.svelte`: All modal text — title, labels, buttons — use `api_keys.*` and `common.*` keys. "Create API Key" -> `$t('api_keys.create')`, "Save" -> `$t('common.save')`, "Cancel" -> `$t('common.cancel')`, "Smart Routing" -> `$t('api_keys.smart_routing')`
- `KeyCreatedModal.svelte`: Success text — use `api_keys.key_created.*` keys
- `RateLimitFields.svelte`: "RPM" -> `$t('api_keys.rate_limits.rpm')`, "TPM" -> `$t('api_keys.rate_limits.tpm')`, help text -> `$t('api_keys.rate_limits.rpm_help')`, `$t('api_keys.rate_limits.tpm_help')`
- `SmartRoutingToggle.svelte`: Toggle label -> `$t('api_keys.smart_routing')`

**Budget components:**
- `BudgetBanner.svelte`: Warning text -> `$t('budget.warning_banner')`, exceeded text -> `$t('budget.exceeded_banner')`
- `BudgetDefaultsForm.svelte`: Form labels -> `budget.defaults.*` keys
- `BudgetPanel.svelte`: Panel text -> `budget.*` keys. "Hard Limit" -> `$t('budget.hard_limit')`, "Soft Limit" -> `$t('budget.soft_limit')`

**Member components:**
- `InvitePanel.svelte`: "Invite" -> `$t('members.invite')`, email label -> `$t('members.invite_email')`, role label -> `$t('members.invite_role')`, submit -> `$t('members.invite_submit')`, sending -> `$t('members.invite_sending')`
- `MemberActionMenu.svelte`: "Remove Member" -> `$t('members.remove_member')`, "Change Role" -> `$t('members.change_role')`
- `MembersTable.svelte`: Table headers -> `common.name`, `common.email`, `common.role`, `common.actions`
- `RoleBadge.svelte`: Role labels — keep dynamic (role names come from data), but if there are static labels like "Owner"/"Admin"/"Member", use `$t()` only if those strings exist as translation keys; otherwise leave as-is since role names may be stored in the database

**Model components:**
- `ModelPricingTable.svelte`: "Model" -> `$t('models.table.name')`, "Provider" -> `$t('models.table.provider')`, "Input Price" -> `$t('models.table.input_price')`, "Output Price" -> `$t('models.table.output_price')`, "per million tokens" -> `$t('models.per_million_tokens')`

**Settings components:**
- `CacheTtlSetting.svelte`: Label -> `$t('settings.cache_ttl.title')`, description -> `$t('settings.cache_ttl.description')`, "seconds" -> `$t('settings.cache_ttl.seconds')`
- `OrgSettingsForm.svelte`: "Organization Name" -> `$t('settings.org_name')`, "Description" -> `$t('settings.org_description')`, "Save Settings" -> `$t('settings.save_settings')`
- `SmartRoutingSettings.svelte`: Title -> `$t('settings.smart_routing.title')`, description -> `$t('settings.smart_routing.description')`, "Cheap Model" -> `$t('settings.smart_routing.cheap_model')`, "Expensive Model" -> `$t('settings.smart_routing.expensive_model')`

**Usage components:**
- `TimeRangePicker.svelte`: "7d" / "30d" / "90d" labels -> `$t('usage.time_range.7d')`, `$t('usage.time_range.30d')`, `$t('usage.time_range.90d')`
- `CostTrendChart.svelte`: Chart title -> `$t('usage.cost_trend.title')`
- `UsageTable.svelte`: Headers -> `$t('usage.table.model')`, `$t('usage.table.provider')`, `$t('usage.table.requests')`, `$t('usage.table.input_tokens')`, `$t('usage.table.output_tokens')`, `$t('usage.table.cost')`, `$t('usage.table.user')`, `$t('usage.table.key')`
- `BreakdownBarChart.svelte`: Title -> `$t('usage.breakdown.title')`
- `UsageTabs.svelte`: Tab labels -> `$t('usage.tabs.overview')`, `$t('usage.tabs.by_model')`, `$t('usage.tabs.by_user')`, `$t('usage.tabs.by_key')`
- `KpiCard.svelte`: If this component has static labels, translate them. If it only receives labels as props, no changes needed (the parent page provides translated props).

**Docs components:**
- `IntegrationGuide.svelte`: Title -> `$t('docs.integration_guide.title')`, description -> `$t('docs.integration_guide.description')`
- `ToolTabs.svelte`: Tab labels -> `$t('docs.tool_tabs.cursor')`, `$t('docs.tool_tabs.continue')`, `$t('docs.tool_tabs.claude_code')`, `$t('docs.tool_tabs.openai_sdk')`

3. For components that display dates, replace hardcoded `'en-US'` locale in `toLocaleDateString()` / `toLocaleString()` / `Intl.DateTimeFormat` / `Intl.NumberFormat` calls with the dynamic `$locale` value. Import `locale` from `svelte-i18n` and use `$locale` as the locale parameter. Example:
   - Before: `d.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`
   - After: `d.toLocaleDateString($locale ?? 'en', { month: 'short', day: 'numeric', year: 'numeric' })`

4. For components that display "Never" for null dates, replace with `$t('common.never')`.

5. For components that display "Loading..." text, replace with `$t('common.loading')`.

6. Do NOT translate: CSS class names, component prop names, HTML attribute values (except aria-label which should use `$t()`), code examples in docs components, variable names, or dynamic data from the database.

7. Do NOT use `{@html $t('key')}` — always use plain `{$t('key')}` to avoid Svelte 5 hydration mismatches.
  </action>
  <verify>
    <automated>npx svelte-kit sync && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Every file in src/lib/components/{layout,dashboard,api-keys,budget,members,models,settings,usage,docs}/*.svelte contains `import { t } from 'svelte-i18n'` or `import { t, locale } from 'svelte-i18n'`
    - src/lib/components/layout/Sidebar.svelte contains `$t('nav.dashboard')` and `$t('nav.settings')`
    - src/lib/components/layout/OrgSwitcher.svelte contains `$t('org_switcher.create_new')`
    - src/lib/components/dashboard/AdminKpiCards.svelte contains `$t('kpi.members')`
    - src/lib/components/dashboard/OnboardingChecklist.svelte contains `$t('onboarding.get_started')`
    - src/lib/components/api-keys/CreateKeyModal.svelte contains `$t('api_keys.create')`
    - src/lib/components/members/InvitePanel.svelte contains `$t('members.invite')`
    - src/lib/components/budget/BudgetPanel.svelte contains `$t('budget.`
    - src/lib/components/models/ModelPricingTable.svelte contains `$t('models.table.`
    - src/lib/components/settings/OrgSettingsForm.svelte contains `$t('settings.org_name')`
    - src/lib/components/usage/UsageTable.svelte contains `$t('usage.table.`
    - src/lib/components/usage/UsageTabs.svelte contains `$t('usage.tabs.`
    - No component contains hardcoded `'en-US'` in toLocaleDateString or toLocaleString calls
    - svelte-check exits with 0 errors
  </acceptance_criteria>
  <done>All 28 dashboard components use $t() for every user-facing string. Nav labels, table headers, button text, empty states, form labels, chart titles, and tab labels all render from translation keys. Date and number formatting uses locale-aware Intl APIs with $locale instead of hardcoded 'en-US'.</done>
</task>

<task type="auto">
  <name>Task 2: Wire $t() into page routes and migrate server action errors to translatable keys</name>
  <files>
    src/routes/org/[slug]/dashboard/+page.svelte,
    src/routes/org/[slug]/api-keys/+page.svelte,
    src/routes/org/[slug]/members/+page.svelte,
    src/routes/org/[slug]/provider-keys/+page.svelte,
    src/routes/org/[slug]/usage/+page.svelte,
    src/routes/org/[slug]/settings/+page.svelte,
    src/routes/org/[slug]/models/+page.svelte,
    src/routes/org/create/+page.svelte,
    src/routes/org/[slug]/+layout.svelte,
    src/routes/org/[slug]/api-keys/+page.server.ts,
    src/routes/org/[slug]/members/+page.server.ts,
    src/routes/org/[slug]/provider-keys/+page.server.ts,
    src/routes/org/[slug]/settings/+page.server.ts,
    src/routes/org/[slug]/usage/budget/+server.ts,
    src/routes/org/create/+page.server.ts
  </files>
  <read_first>
    src/lib/i18n/en.json,
    src/routes/org/[slug]/dashboard/+page.svelte,
    src/routes/org/[slug]/api-keys/+page.svelte,
    src/routes/org/[slug]/api-keys/+page.server.ts,
    src/routes/org/[slug]/members/+page.svelte,
    src/routes/org/[slug]/members/+page.server.ts,
    src/routes/org/[slug]/provider-keys/+page.svelte,
    src/routes/org/[slug]/provider-keys/+page.server.ts,
    src/routes/org/[slug]/usage/+page.svelte,
    src/routes/org/[slug]/settings/+page.svelte,
    src/routes/org/[slug]/settings/+page.server.ts,
    src/routes/org/[slug]/models/+page.svelte,
    src/routes/org/create/+page.svelte,
    src/routes/org/create/+page.server.ts,
    src/routes/org/[slug]/+layout.svelte,
    src/routes/org/[slug]/usage/budget/+server.ts
  </read_first>
  <action>
**Part A: Wire $t() into page route .svelte files (9 files)**

For each page route, add `import { t, locale } from 'svelte-i18n';` and replace all hardcoded English strings with `$t()` calls:

1. `src/routes/org/[slug]/dashboard/+page.svelte`:
   - `<title>` tag: `{$t('dashboard.title')} - StarAIGateway` (keep "StarAIGateway" untranslated — it's a brand name)
   - "Manage Members" -> `$t('dashboard.manage_members')`
   - "View API Keys" -> `$t('dashboard.view_api_keys')`
   - "Budget Settings" -> `$t('dashboard.budget_settings')`
   - Empty dashboard message -> `$t('dashboard.empty_message')`

2. `src/routes/org/[slug]/api-keys/+page.svelte`:
   - `<title>`: `{$t('api_keys.title')} - StarAIGateway`
   - "API Keys" heading -> `$t('api_keys.title')`
   - Description paragraph -> `$t('api_keys.description')`
   - "Create API Key" button -> `$t('api_keys.create')`
   - "My Keys" tab -> `$t('api_keys.my_keys')`
   - "All Keys" tab -> `$t('api_keys.all_keys')`
   - "No API keys yet" -> `$t('api_keys.no_keys')`
   - "Create one to get started." -> `$t('api_keys.no_keys_hint')`
   - "No API keys in this organization" -> `$t('api_keys.no_org_keys')`
   - Table headers: "Name" -> `$t('api_keys.table.name')`, "Key" -> `$t('api_keys.table.key')`, "Status" -> `$t('api_keys.table.status')`, "Created" -> `$t('api_keys.table.created')`, "Last Used" -> `$t('api_keys.table.last_used')`, "Owner" -> `$t('api_keys.table.owner')`, "RPM" -> `$t('api_keys.table.rpm')`, "TPM" -> `$t('api_keys.table.tpm')`
   - "Active" badge -> `$t('common.active')`
   - "Revoked" badge -> `$t('common.revoked')`
   - "Revoke" button -> `$t('api_keys.revoke')`
   - "Revoking..." -> `$t('api_keys.revoking')`
   - confirm() dialog text -> `$t('api_keys.revoke_confirm')` — NOTE: `confirm()` is a browser native dialog that only accepts strings, so use `confirm($t('api_keys.revoke_confirm'))` which works because `$t()` returns a string
   - "Edit Rate Limits" -> `$t('api_keys.edit_rate_limits')`
   - "Save" -> `$t('common.save')`
   - "Cancel" -> `$t('common.cancel')`
   - "Never" in formatDate -> `$t('common.never')` — update the formatDate function to use `$t('common.never')` for null dates. Since `$t` is a store, access it inside the template or make formatDate accept a neverLabel parameter: `function formatDate(date, neverLabel)` and call as `formatDate(key.createdAt, $t('common.never'))`
   - Replace `'en-US'` in `toLocaleDateString` with `$locale ?? 'en'`

3. `src/routes/org/[slug]/members/+page.svelte`:
   - `<title>`: `{$t('members.title')} - StarAIGateway`
   - Page heading -> `$t('members.title')`
   - All table headers, button labels, and empty states -> `members.*` keys
   - "Pending Invitations" -> `$t('members.pending_invitations')`
   - confirm() dialogs -> use `$t('members.remove_confirm')` for remove member

4. `src/routes/org/[slug]/provider-keys/+page.svelte`:
   - `<title>`: `{$t('provider_keys.title')} - StarAIGateway`
   - All provider-related text -> `provider_keys.*` keys
   - Provider names like "OpenAI", "Anthropic" -> `$t('provider_keys.providers.openai')`, `$t('provider_keys.providers.anthropic')`, etc.
   - "Add Key" -> `$t('provider_keys.add_key')`
   - "No provider keys" -> `$t('provider_keys.no_keys')`

5. `src/routes/org/[slug]/usage/+page.svelte`:
   - `<title>`: `{$t('usage.title')} - StarAIGateway`
   - "Usage" heading -> `$t('usage.title')`
   - "No data" -> `$t('usage.no_data')`

6. `src/routes/org/[slug]/settings/+page.svelte`:
   - `<title>`: `{$t('settings.title')} - StarAIGateway`
   - "Settings" heading -> `$t('settings.title')`
   - "Danger Zone" -> `$t('settings.danger_zone')`

7. `src/routes/org/[slug]/models/+page.svelte`:
   - `<title>`: `{$t('models.title')} - StarAIGateway`
   - "Models" heading -> `$t('models.title')`
   - "No models" -> `$t('models.no_models')`

8. `src/routes/org/create/+page.svelte`:
   - `<title>`: `{$t('org_create.title')} - StarAIGateway`
   - Form labels -> `org_create.*` keys
   - "Name" label -> `$t('org_create.name_label')`
   - "Description" label -> `$t('org_create.description_label')`
   - Submit button -> `$t('org_create.submit')` / `$t('org_create.creating')` when loading
   - Error display: change `{form.error}` to `{$t(form.errorKey)}` (see Part B below)

9. `src/routes/org/[slug]/+layout.svelte`:
   - If this layout has any hardcoded text, translate it. It likely has minimal text — just import `t` if needed.

**Part B: Migrate server action errors to translatable keys (6 server files)**

For each `+page.server.ts` / `+server.ts` file, change `fail()` and `error()` calls that return user-facing error messages from hardcoded English strings to i18n error keys:

1. `src/routes/org/[slug]/api-keys/+page.server.ts`:
   - `return fail(400, { error: 'Failed to create API key' })` -> `return fail(400, { errorKey: 'errors.create_key_failed' })`
   - `return fail(403, { error: 'Only owners and admins can revoke keys' })` -> `return fail(403, { errorKey: 'errors.only_admins_revoke_keys' })`
   - `return fail(404, { error: 'API key not found' })` -> `return fail(404, { errorKey: 'errors.key_not_found' })`
   - `return fail(404, { error: 'API key not found in this organization' })` -> `return fail(404, { errorKey: 'errors.key_not_in_org' })`
   - `return fail(403, { error: 'Only owners and admins can update rate limits' })` -> `return fail(403, { errorKey: 'errors.only_admins_rate_limits' })`
   - For Zod validation errors (`return fail(400, { error: parsed.error.errors[0].message })`): Map Zod error messages to i18n keys. Create a helper function at the top of the file or in a shared location:
     ```typescript
     function zodErrorToKey(zodMessage: string): string {
       const map: Record<string, string> = {
         'Name is required': 'validation.name_required',
         'Name must be 50 characters or less': 'validation.name_too_long',
         'Please enter a valid email address': 'validation.email_invalid',
         'Key ID is required': 'validation.key_id_required',
         'Invalid role': 'validation.invalid_role',
       };
       return map[zodMessage] ?? 'errors.generic';
     }
     ```
     Then: `return fail(400, { errorKey: zodErrorToKey(parsed.error.errors[0].message) })`

2. `src/routes/org/[slug]/members/+page.server.ts`:
   - `'Only admins can invite members'` -> `errorKey: 'errors.only_admins_invite'`
   - `'Invalid role'` -> `errorKey: 'validation.invalid_role'`
   - `'Failed to send invitation'` -> `errorKey: 'errors.invite_failed'`
   - `'Only the owner can change roles'` -> `errorKey: 'errors.only_owner_change_role'`
   - `'Failed to change role'` -> `errorKey: 'errors.role_change_failed'`
   - `'Only admins can remove members'` -> `errorKey: 'errors.only_admins_remove'`
   - `'Failed to remove member'` -> `errorKey: 'errors.remove_failed'`
   - `'Only admins can revoke invitations'` -> `errorKey: 'errors.only_admins_revoke_invite'`
   - Apply zodErrorToKey for Zod validation errors

3. `src/routes/org/[slug]/provider-keys/+page.server.ts`:
   - `'Provider key not found'` -> `errorKey: 'errors.key_not_found'`
   - `'Failed to create provider key'` -> `errorKey: 'errors.provider_key_failed'`
   - Apply zodErrorToKey for Zod validation errors

4. `src/routes/org/[slug]/settings/+page.server.ts`:
   - `'Only owners and admins can update settings'` -> `errorKey: 'errors.settings_failed'`
   - `'RPM limit must be a positive number'` -> `errorKey: 'validation.name_required'` — actually, add specific keys if they exist. If the en.json from Plan 01 does not have a specific key, use the closest match or `errors.settings_failed`
   - `'TTL must be between 60 and 86,400 seconds.'` -> `errorKey: 'errors.settings_failed'` or a more specific validation key

5. `src/routes/org/[slug]/usage/budget/+server.ts`:
   - `error(403, 'Only admins and owners can manage budgets')` -> `error(403, 'errors.budget_failed')` — NOTE: SvelteKit's `error()` function from `@sveltejs/kit` sends HTTP error responses and is NOT displayed as form error. These are HTTP-level errors that show the SvelteKit error page. Leave `error()` calls as-is (they are not user-facing form validation). Only change `fail()` calls which return data to form actions.
   - If this file uses `fail()`, change those. If it only uses `error()`, leave it unchanged — `error()` throws HTTP errors that show the error page, not inline form errors.

6. `src/routes/org/create/+page.server.ts`:
   - Change `fail(400, { error: '...' })` to `fail(400, { errorKey: '...' })`
   - Map error messages to `validation.*` or `errors.*` keys

**Part C: Update page .svelte files to display translated errors**

For each page that displays form action errors, update the error display to use `$t()`:

- `src/routes/org/create/+page.svelte`: Change `{form.error}` to `{$t(form.errorKey)}` and update the `{#if form?.error}` check to `{#if form?.errorKey}`
- For other pages that use `use:enhance` with error handling — check if they display errors inline. If they do, apply the same `$t(form.errorKey)` pattern. If errors are silently handled (no visible error display), no client-side changes needed.
- Add a generic error display pattern near form elements in pages that currently have no error display but receive `fail()` responses: add `{#if form?.errorKey}<p class="mt-2 text-sm text-red-400">{$t(form.errorKey)}</p>{/if}` near the relevant form.

**Important notes:**
- Keep "StarAIGateway" untranslated everywhere — it is a brand name.
- Keep `aria-label` attributes translated using `$t()` for accessibility.
- The `zodErrorToKey` helper function should be placed in a shared file `src/lib/server/i18n-errors.ts` so all server files can import it, avoiding duplication.
  </action>
  <verify>
    <automated>npx svelte-kit sync && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 page .svelte files under src/routes/org/ contain `import { t } from 'svelte-i18n'` or `import { t, locale } from 'svelte-i18n'`
    - src/routes/org/[slug]/api-keys/+page.svelte contains `$t('api_keys.title')` and `$t('api_keys.create')`
    - src/routes/org/[slug]/api-keys/+page.svelte contains `$t('common.active')` and `$t('common.revoked')`
    - src/routes/org/[slug]/api-keys/+page.svelte does NOT contain hardcoded string "No API keys yet" (replaced with $t call)
    - src/routes/org/[slug]/members/+page.svelte contains `$t('members.title')`
    - src/routes/org/[slug]/provider-keys/+page.svelte contains `$t('provider_keys.title')`
    - src/routes/org/[slug]/usage/+page.svelte contains `$t('usage.title')`
    - src/routes/org/[slug]/settings/+page.svelte contains `$t('settings.title')`
    - src/routes/org/[slug]/models/+page.svelte contains `$t('models.title')`
    - src/routes/org/create/+page.svelte contains `$t('org_create.title')`
    - src/routes/org/create/+page.svelte contains `$t(form.errorKey)` (not `form.error`)
    - src/routes/org/[slug]/api-keys/+page.server.ts contains `errorKey:` (not `error:` for user-facing messages)
    - src/routes/org/[slug]/members/+page.server.ts contains `errorKey:` (not `error:` for user-facing messages)
    - src/routes/org/[slug]/provider-keys/+page.server.ts contains `errorKey:`
    - src/routes/org/create/+page.server.ts contains `errorKey:`
    - A shared zodErrorToKey function exists (either in src/lib/server/i18n-errors.ts or inline in each server file)
    - No page .svelte file contains hardcoded `'en-US'` in formatting calls
    - svelte-check exits with 0 errors
  </acceptance_criteria>
  <done>All 9 dashboard page routes use $t() for every user-facing string. All 6 server action files return errorKey instead of hardcoded English error strings. Page svelte files display translated error messages via $t(form.errorKey). The zodErrorToKey helper maps Zod validation messages to i18n keys. Dates and numbers use locale-aware formatting throughout.</done>
</task>

</tasks>

<verification>
1. `npx svelte-kit sync && npx svelte-check --tsconfig ./tsconfig.json` — no type errors
2. `grep -r "import { t" src/lib/components/ --include="*.svelte" | wc -l` — should be >= 27 (all components with user-facing text)
3. `grep -r "import { t" src/routes/org/ --include="*.svelte" | wc -l` — should be >= 9 (all page routes)
4. `grep -rL "import.*svelte-i18n" src/lib/components/{layout,dashboard,api-keys,budget,members,models,settings,usage,docs}/*.svelte` — should return empty (no components missing i18n import)
5. `grep -r "errorKey:" src/routes/org/ --include="*.server.ts" | wc -l` — should be >= 10 (all error returns use errorKey)
6. `grep -rn "'en-US'" src/lib/components/ src/routes/org/ --include="*.svelte"` — should return empty (no hardcoded locale)
</verification>

<success_criteria>
- Every dashboard component (.svelte file under src/lib/components/) uses $t() for all user-facing strings — no hardcoded English text remains
- Every dashboard page route (.svelte file under src/routes/org/) uses $t() for page titles, headings, labels, empty states, and button text
- All server action error messages use errorKey field with i18n keys instead of hardcoded English strings
- Client-side error display uses $t(form.errorKey) to show translated error messages
- Date and number formatting uses $locale instead of hardcoded 'en-US'
- svelte-check passes with 0 errors
- Switching language via the TopBar switcher (from Plan 01) causes ALL dashboard text to update reactively
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-internationalization/12-02-SUMMARY.md`
</output>
