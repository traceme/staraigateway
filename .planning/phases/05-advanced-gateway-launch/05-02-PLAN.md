---
phase: 05-advanced-gateway-launch
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/routes/org/[slug]/settings/+page.svelte
  - src/routes/org/[slug]/settings/+page.server.ts
  - src/lib/components/settings/SmartRoutingSettings.svelte
  - src/lib/components/settings/CacheTtlSetting.svelte
  - src/lib/components/api-keys/SmartRoutingToggle.svelte
  - src/lib/components/api-keys/CreateKeyModal.svelte
autonomous: true
requirements: [GW-08, GW-09]

must_haves:
  truths:
    - "Admin can configure cheap and expensive model names in org settings"
    - "Admin can configure cache TTL in org settings"
    - "Smart routing toggle appears in API key creation modal"
    - "Toggle is disabled when org has no smart routing models configured"
    - "No Redis banner shows when REDIS_URL is not configured"
  artifacts:
    - path: "src/lib/components/settings/SmartRoutingSettings.svelte"
      provides: "Smart routing model configuration form section"
      contains: "Save Routing Settings"
    - path: "src/lib/components/settings/CacheTtlSetting.svelte"
      provides: "Cache TTL configuration form section"
      contains: "Save Cache Settings"
    - path: "src/lib/components/api-keys/SmartRoutingToggle.svelte"
      provides: "Toggle switch for enabling smart routing on API key"
      contains: "Enable smart routing"
  key_links:
    - from: "src/routes/org/[slug]/settings/+page.svelte"
      to: "SmartRoutingSettings.svelte"
      via: "component import and rendering"
      pattern: "SmartRoutingSettings"
    - from: "src/routes/org/[slug]/settings/+page.svelte"
      to: "CacheTtlSetting.svelte"
      via: "component import and rendering"
      pattern: "CacheTtlSetting"
    - from: "src/lib/components/api-keys/CreateKeyModal.svelte"
      to: "SmartRoutingToggle.svelte"
      via: "component import inside modal"
      pattern: "SmartRoutingToggle"
---

<objective>
Add UI for configuring smart routing model tiers and cache TTL in org settings, and add smart routing toggle to API key creation modal.

Purpose: Allow admins to configure the gateway features built in Plan 01 through the dashboard UI.
Output: 3 new Svelte components, 2 modified pages/components, new form actions for saving settings.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-advanced-gateway-launch/05-CONTEXT.md
@.planning/phases/05-advanced-gateway-launch/05-UI-SPEC.md
@.planning/phases/05-advanced-gateway-launch/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smart routing settings, cache TTL settings components, and form actions on org settings page</name>
  <files>
    src/lib/components/settings/SmartRoutingSettings.svelte,
    src/lib/components/settings/CacheTtlSetting.svelte,
    src/routes/org/[slug]/settings/+page.server.ts,
    src/routes/org/[slug]/settings/+page.svelte
  </files>
  <read_first>
    src/routes/org/[slug]/settings/+page.server.ts,
    src/routes/org/[slug]/settings/+page.svelte,
    src/lib/server/db/schema.ts,
    src/lib/server/redis.ts,
    .planning/phases/05-advanced-gateway-launch/05-UI-SPEC.md
  </read_first>
  <action>
    1. Create `src/lib/components/settings/SmartRoutingSettings.svelte`:
       Props: `cheapModel: string | null`, `expensiveModel: string | null`, `orgSlug: string`
       - Card container: `rounded-lg border border-zinc-800 bg-zinc-900/50 p-6`
       - Heading: "Smart Routing" in `text-lg font-bold text-zinc-100`
       - Description: "Configure which models are used for simple and complex queries when smart routing is enabled." in `mt-1 text-sm text-zinc-400`
       - Two text inputs: `name="cheapModel"` (placeholder "e.g., gpt-4o-mini", label "Cheap model (simple queries)", helper "Used for queries under 500 tokens.") and `name="expensiveModel"` (placeholder "e.g., gpt-4o", label "Expensive model (complex queries)", helper "Used for queries of 500 tokens or more.")
       - Input styling: `rounded-md border border-zinc-700 bg-zinc-800 px-3 py-2 text-sm text-zinc-200 placeholder-zinc-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500`
       - Form with `method="POST"` action `?/saveRouting` and `use:enhance`
       - Submit button: "Save Routing Settings" with saving/success states
       - Success feedback: green "Settings saved" text next to button, fades after 3s
       - aria-label on inputs: "Cheap model for simple queries" and "Expensive model for complex queries"

    2. Create `src/lib/components/settings/CacheTtlSetting.svelte`:
       Props: `cacheTtlSeconds: number`, `orgSlug: string`, `redisAvailable: boolean`
       - Card container: same styling as SmartRoutingSettings
       - Heading: "Response Caching"
       - Description: "Cache identical non-streaming responses to reduce API calls. Requires Redis."
       - If `!redisAvailable`: show info banner `bg-blue-900/20 border border-blue-500/30 rounded-lg p-4 text-sm text-zinc-400` with text "Response caching requires Redis. Set the REDIS_URL environment variable to enable caching."
       - Number input: `name="cacheTtlSeconds"`, `min="60"`, `max="86400"`, label "Cache TTL (seconds)", helper "How long cached responses are stored. Default: 3600 seconds (1 hour)."
       - aria-label: "Cache time to live in seconds"
       - Inline validation error: "TTL must be between 60 and 86,400 seconds." when out of range
       - Form action: `?/saveCacheTtl`, button "Save Cache Settings"
       - Same saving/success states as SmartRoutingSettings

    3. Update `src/routes/org/[slug]/settings/+page.server.ts`:
       - In the `load` function: add `smartRoutingCheapModel`, `smartRoutingExpensiveModel`, `cacheTtlSeconds` to the returned org data
       - Add `redisAvailable: Boolean(env.REDIS_URL)` using import from `$env/dynamic/private` (check at load time, pass to page)
       - Add form action `saveRouting`: validate and update `appOrganizations` set `smartRoutingCheapModel` and `smartRoutingExpensiveModel` where orgId matches. Both fields can be null/empty string (clears routing).
       - Add form action `saveCacheTtl`: validate cacheTtlSeconds is integer between 60 and 86400, update `appOrganizations` set `cacheTtlSeconds` where orgId matches.
       - Both actions require admin/owner role (same pattern as existing settings actions)

    4. Update `src/routes/org/[slug]/settings/+page.svelte`:
       - Import SmartRoutingSettings and CacheTtlSetting
       - Add them below the existing settings form, separated by `space-y-8` gap
       - Pass org data as props: cheapModel, expensiveModel, cacheTtlSeconds, orgSlug, redisAvailable

    5. Run `npm run build` to verify no type errors.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Save Routing Settings" src/lib/components/settings/SmartRoutingSettings.svelte returns 0
    - grep -q "Save Cache Settings" src/lib/components/settings/CacheTtlSetting.svelte returns 0
    - grep -q "saveRouting" src/routes/org/\[slug\]/settings/+page.server.ts returns 0
    - grep -q "saveCacheTtl" src/routes/org/\[slug\]/settings/+page.server.ts returns 0
    - grep -q "SmartRoutingSettings" src/routes/org/\[slug\]/settings/+page.svelte returns 0
    - grep -q "CacheTtlSetting" src/routes/org/\[slug\]/settings/+page.svelte returns 0
    - grep -q "redisAvailable" src/routes/org/\[slug\]/settings/+page.server.ts returns 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>Org settings page has Smart Routing section (cheap/expensive model inputs with save) and Response Caching section (TTL input with save). Redis availability banner shows when REDIS_URL not set.</done>
</task>

<task type="auto">
  <name>Task 2: Smart routing toggle in API key creation modal</name>
  <files>
    src/lib/components/api-keys/SmartRoutingToggle.svelte,
    src/lib/components/api-keys/CreateKeyModal.svelte
  </files>
  <read_first>
    src/lib/components/api-keys/CreateKeyModal.svelte,
    src/lib/components/api-keys/RateLimitFields.svelte,
    src/lib/server/db/schema.ts,
    .planning/phases/05-advanced-gateway-launch/05-UI-SPEC.md
  </read_first>
  <action>
    1. Create `src/lib/components/api-keys/SmartRoutingToggle.svelte`:
       Props: `enabled: boolean` (bindable, default false), `orgHasRouting: boolean`
       - Container: `flex items-start gap-3 rounded-lg border border-zinc-800 bg-zinc-900/50 p-4 mt-4`
       - Toggle switch button: 44px wide (`w-11`), 24px tall (`h-6`), `rounded-full`, `role="switch"`, `aria-checked={enabled}`, `aria-label="Enable smart routing"`
         - Off state: `bg-zinc-700` track
         - On state: `bg-blue-600` track
         - Knob: `w-5 h-5 bg-white rounded-full shadow transition-transform duration-200`
         - Off knob: `translate-x-0.5`
         - On knob: `translate-x-[22px]`
         - Transition: `transition-colors duration-200` on track
       - Click handler toggles `enabled` (only if `orgHasRouting` is true)
       - Keyboard: Space and Enter toggle the switch
       - Focus ring: `focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-offset-zinc-900`
       - Label: "Enable smart routing" in `text-sm font-bold text-zinc-200`
       - Description when orgHasRouting: "Automatically route simple queries to cheaper models." in `text-xs text-zinc-500 mt-1`
       - Description when !orgHasRouting: "Configure smart routing models in organization settings first." in `text-xs text-zinc-500 mt-1`
       - When !orgHasRouting: add `opacity-50 cursor-not-allowed` to container, `aria-disabled="true"` and `tabindex="-1"` on toggle button
       - Hidden input: `<input type="hidden" name="smartRouting" value={enabled ? 'true' : 'false'} />`

    2. Modify `src/lib/components/api-keys/CreateKeyModal.svelte`:
       - Import SmartRoutingToggle
       - Add prop: `orgHasRouting: boolean` (passed from parent — true when org's smartRoutingCheapModel AND smartRoutingExpensiveModel are both non-null/non-empty)
       - Place `<SmartRoutingToggle bind:enabled={smartRoutingEnabled} {orgHasRouting} />` between the rate limit fields and the action buttons
       - Initialize `let smartRoutingEnabled = $state(false)`
       - The hidden input in SmartRoutingToggle will be submitted with the form
       - In the form action handler that creates the key, read `smartRouting` from form data and pass to the insert query

    3. Update the API key creation form action (in the relevant +page.server.ts for api-keys) to read `formData.get('smartRouting') === 'true'` and include `smartRouting: boolean` in the insert to `appApiKeys`.

    4. Run `npm run build` to verify.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Enable smart routing" src/lib/components/api-keys/SmartRoutingToggle.svelte returns 0
    - grep -q 'role="switch"' src/lib/components/api-keys/SmartRoutingToggle.svelte returns 0
    - grep -q "aria-checked" src/lib/components/api-keys/SmartRoutingToggle.svelte returns 0
    - grep -q "SmartRoutingToggle" src/lib/components/api-keys/CreateKeyModal.svelte returns 0
    - grep -q 'name="smartRouting"' src/lib/components/api-keys/SmartRoutingToggle.svelte returns 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>API key creation modal includes a smart routing toggle. Toggle is interactive when org has routing configured, disabled with explanation when not. smartRouting boolean is persisted to the API key record on creation.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Settings page shows Smart Routing and Response Caching sections
- API key modal shows smart routing toggle
- `npx vitest run` still passes (no regressions)
</verification>

<success_criteria>
- Admin can set cheap/expensive model names and save
- Admin can set cache TTL and save
- Redis unavailability banner displays when REDIS_URL not set
- Smart routing toggle in key creation modal works with proper enabled/disabled states
- All form actions validate input and persist to database
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-gateway-launch/05-02-SUMMARY.md`
</output>
