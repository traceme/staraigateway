---
phase: 05-advanced-gateway-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
  - src/routes/+page.server.ts
  - src/lib/components/landing/LandingNav.svelte
  - src/lib/components/landing/LandingHero.svelte
  - src/lib/components/landing/FeaturesGrid.svelte
  - src/lib/components/landing/CostComparison.svelte
  - src/lib/components/landing/SelfHostCta.svelte
  - src/lib/components/landing/LandingFooter.svelte
autonomous: true
requirements: [SHIP-01]

must_haves:
  truths:
    - "Unauthenticated users see landing page at /"
    - "Authenticated users visiting / are redirected to their org dashboard"
    - "Landing page has hero, features grid, cost comparison, self-host CTA, and footer"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Landing page with all sections"
      contains: "Share AI access with your team"
    - path: "src/lib/components/landing/LandingHero.svelte"
      provides: "Hero section with value proposition"
      contains: "Get Started Free"
    - path: "src/lib/components/landing/FeaturesGrid.svelte"
      provides: "6 feature cards in responsive grid"
      contains: "Everything you need"
  key_links:
    - from: "src/routes/+page.server.ts"
      to: "org dashboard"
      via: "redirect when authenticated"
      pattern: "redirect"
---

<objective>
Create the landing page with hero, features grid, cost comparison, self-host CTA, and footer. Redirect authenticated users to their org dashboard.

Purpose: Give visitors a compelling first impression of the product with clear value proposition and call to action.
Output: 8 files — landing page route, server load function, 6 landing section components.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-advanced-gateway-launch/05-CONTEXT.md
@.planning/phases/05-advanced-gateway-launch/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page with all sections and auth redirect</name>
  <files>
    src/routes/+page.server.ts,
    src/routes/+page.svelte,
    src/lib/components/landing/LandingNav.svelte,
    src/lib/components/landing/LandingHero.svelte,
    src/lib/components/landing/FeaturesGrid.svelte,
    src/lib/components/landing/CostComparison.svelte,
    src/lib/components/landing/SelfHostCta.svelte,
    src/lib/components/landing/LandingFooter.svelte
  </files>
  <read_first>
    src/routes/+page.svelte,
    src/routes/+page.server.ts,
    src/routes/+layout.server.ts,
    .planning/phases/05-advanced-gateway-launch/05-UI-SPEC.md
  </read_first>
  <action>
    1. Update `src/routes/+page.server.ts`:
       - In the load function, check `locals.session` (or however auth is accessed in this project)
       - If user is authenticated and has an org, `throw redirect(302, '/org/' + userOrg.slug + '/dashboard')`
       - If not authenticated, return `{ authenticated: false }` (landing page renders)
       - Import `redirect` from `@sveltejs/kit`

    2. Create `src/lib/components/landing/LandingNav.svelte`:
       - Fixed top nav: `fixed top-0 w-full z-50 bg-zinc-950/80 backdrop-blur-sm border-b border-zinc-800`
       - Inner: `max-w-6xl mx-auto px-6 h-14 flex items-center justify-between`
       - Left: "LLMTokenHub" in `text-lg font-bold text-zinc-50`
       - Right (unauthenticated): "Log In" link (`href="/auth/login"`, `text-zinc-400 hover:text-zinc-100 text-sm`) + "Get Started" button (`href="/auth/signup"`, `bg-blue-600 hover:bg-blue-700 text-white text-sm rounded-lg px-4 py-2`)
       - `<nav aria-label="Main navigation">`

    3. Create `src/lib/components/landing/LandingHero.svelte`:
       - Background: `bg-gradient-to-b from-blue-900/20 to-zinc-950`
       - Padding: `pt-24 pb-16` (clears fixed nav)
       - Inner: `max-w-4xl mx-auto px-6 text-center`
       - Title: "Share AI access with your team. Save 40-60% on LLM costs." in `text-5xl font-bold text-zinc-50` (mobile: `text-3xl`)
       - Subtitle: "Pool API keys across your team instead of buying per-seat subscriptions. Budget controls, smart routing, and usage tracking built in." in `text-xl text-zinc-400 max-w-2xl mx-auto mt-6`
       - CTA: "Get Started Free" linking to `/auth/signup`, `bg-blue-600 hover:bg-blue-700 text-white text-lg font-bold rounded-lg px-8 py-3 mt-8 inline-block`

    4. Create `src/lib/components/landing/FeaturesGrid.svelte`:
       - Section: `py-16`, inner `max-w-6xl mx-auto px-6`
       - Heading: "Everything you need to manage team AI access" in `text-[30px] font-bold text-zinc-50 text-center mb-12`
       - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`
       - 6 feature cards, each: `bg-zinc-900 rounded-lg border border-zinc-800 p-6`
       - Card icon: inline SVG (Lucide-style, 32px, `text-blue-500 mb-4`), card title `text-lg font-bold text-zinc-100 mb-2`, card description `text-sm text-zinc-400`
       - Cards (title / description): exact copy from UI-SPEC copywriting contract (Bring Your Own Keys, Budget Controls, Smart Routing, Usage Tracking, Response Caching, Works With Your Tools)
       - Icons: key, shield, route (arrows diverging), chart (bar-chart), zap (lightning), code (brackets) — all as inline SVG paths

    5. Create `src/lib/components/landing/CostComparison.svelte`:
       - Section: `py-16 bg-zinc-900/50`
       - Heading: "Stop paying for seats nobody uses" in `text-[30px] font-bold text-zinc-50 text-center mb-12`
       - Grid: `grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto px-6`
       - Left card (Per-Seat): `bg-zinc-900 rounded-lg border border-zinc-800 p-6`, bullet list with items: "ChatGPT Plus: $20/user/month", "GitHub Copilot: $19/user/month", "Claude Pro: $20/user/month", "30 users = $1,770/month"
       - Right card (Pooled): `bg-zinc-900 rounded-lg border border-blue-600/50 p-6` (blue border), checkmark items (`text-green-500` icon): "Pay only for tokens consumed", "80% of usage below subscription break-even", "Smart routing cuts costs further", "30 users ~ $700/month"
       - Savings callout: "Save up to 60% on AI costs" in `text-2xl font-bold text-green-500 text-center mt-8`

    6. Create `src/lib/components/landing/SelfHostCta.svelte`:
       - Section: `py-16`, inner `max-w-4xl mx-auto px-6 text-center`
       - Title: "Deploy on your own infrastructure" in `text-[30px] font-bold text-zinc-50 mb-4`
       - Subtitle: "Self-host LLMTokenHub with Docker Compose. Your data stays on your servers." in `text-zinc-400 mb-8 max-w-xl mx-auto`
       - Code block: `bg-zinc-900 rounded-lg border border-zinc-800 p-4 text-left font-mono text-sm text-zinc-300 max-w-lg mx-auto`
       - Lines: "git clone https://github.com/your-org/llmtokenhub.git" and "cd llmtokenhub && docker compose up -d"
       - Link: "View Self-Host Guide" (`href="/docs/self-host"` or link to docs/self-host.md) with `border border-zinc-700 text-zinc-300 hover:text-zinc-100 hover:border-zinc-600 rounded-lg px-6 py-3 mt-6 inline-block text-sm`

    7. Create `src/lib/components/landing/LandingFooter.svelte`:
       - `border-t border-zinc-800 py-8 px-6`
       - Inner: `max-w-6xl mx-auto flex items-center justify-between`
       - Left: "(c) 2026 LLMTokenHub" in `text-sm text-zinc-500`
       - Right: links "GitHub", "Documentation", "Integration Guides" in `text-sm text-zinc-400 hover:text-zinc-300` with `gap-6`
       - "Integration Guides" links to `/docs/integrations`

    8. Replace `src/routes/+page.svelte`:
       - Import all 6 landing components
       - Render in order: LandingNav, LandingHero, FeaturesGrid, CostComparison, SelfHostCta, LandingFooter
       - Page background: `bg-zinc-950 min-h-screen text-zinc-50`
       - This page should NOT use the app layout (sidebar etc.) — it's a standalone public page

    9. Run `npm run build` to verify.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Share AI access with your team" src/lib/components/landing/LandingHero.svelte returns 0
    - grep -q "Get Started Free" src/lib/components/landing/LandingHero.svelte returns 0
    - grep -q "Everything you need to manage team AI access" src/lib/components/landing/FeaturesGrid.svelte returns 0
    - grep -q "Stop paying for seats nobody uses" src/lib/components/landing/CostComparison.svelte returns 0
    - grep -q "Deploy on your own infrastructure" src/lib/components/landing/SelfHostCta.svelte returns 0
    - grep -q "2026 LLMTokenHub" src/lib/components/landing/LandingFooter.svelte returns 0
    - grep -q "redirect" src/routes/+page.server.ts returns 0
    - grep -q "LandingNav" src/routes/+page.svelte returns 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>Landing page renders at / with hero, features grid, cost comparison, self-host CTA, and footer. Authenticated users are redirected to their org dashboard.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Landing page components exist in `src/lib/components/landing/`
</verification>

<success_criteria>
- Landing page renders hero, features, cost comparison, self-host CTA, and footer
- Authenticated users are redirected from / to their dashboard
- All 6 landing components created with proper styling
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-gateway-launch/05-03-SUMMARY.md`
</output>
