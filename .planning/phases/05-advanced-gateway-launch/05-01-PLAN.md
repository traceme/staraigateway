---
phase: 05-advanced-gateway-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/db/schema.ts
  - src/lib/server/redis.ts
  - src/lib/server/gateway/cache.ts
  - src/lib/server/gateway/routing.ts
  - src/lib/server/gateway/load-balancer.ts
  - src/lib/server/gateway/proxy.ts
  - src/lib/server/gateway/auth.ts
  - vitest.config.ts
  - src/lib/server/gateway/load-balancer.test.ts
  - src/lib/server/gateway/routing.test.ts
  - src/lib/server/gateway/cache.test.ts
  - src/lib/server/gateway/proxy.test.ts
  - package.json
autonomous: true
requirements: [GW-06, GW-07, GW-08, GW-09, GW-10]

must_haves:
  truths:
    - "Gateway auto-retries on 429/500/503 with exponential backoff (3 attempts max)"
    - "After retries exhausted, gateway falls back to next active provider key for same model"
    - "If all keys exhausted, gateway returns the last error to the client"
    - "Smart routing substitutes cheap model for <500 token requests when enabled on API key"
    - "Non-streaming responses are cached in Redis with configurable TTL when REDIS_URL is set"
    - "Cache hits return X-Cache: HIT header and skip provider call"
    - "Round-robin distributes requests across multiple keys for same provider"
    - "App works normally when REDIS_URL is not set (caching silently disabled)"
  artifacts:
    - path: "src/lib/server/gateway/load-balancer.ts"
      provides: "Round-robin key selection with fallback chain"
      exports: ["selectKeyRoundRobin"]
    - path: "src/lib/server/gateway/routing.ts"
      provides: "Token estimation and tier selection"
      exports: ["estimateTokenCount", "selectModelTier"]
    - path: "src/lib/server/gateway/cache.ts"
      provides: "Redis cache get/set/hash for responses"
      exports: ["generateCacheKey", "getCachedResponse", "setCachedResponse"]
    - path: "src/lib/server/redis.ts"
      provides: "Lazy Redis singleton"
      exports: ["getRedis"]
  key_links:
    - from: "src/lib/server/gateway/proxy.ts"
      to: "src/lib/server/gateway/load-balancer.ts"
      via: "selectKeyRoundRobin for key ordering"
      pattern: "selectKeyRoundRobin"
    - from: "src/lib/server/gateway/proxy.ts"
      to: "src/lib/server/gateway/cache.ts"
      via: "cache check before provider call, cache set after success"
      pattern: "getCachedResponse|setCachedResponse"
    - from: "src/lib/server/gateway/proxy.ts"
      to: "src/lib/server/gateway/routing.ts"
      via: "smart routing model substitution"
      pattern: "selectModelTier"
    - from: "src/lib/server/gateway/cache.ts"
      to: "src/lib/server/redis.ts"
      via: "getRedis() for Redis client"
      pattern: "getRedis"
---

<objective>
Production-harden the gateway with retry/fallback, smart routing, Redis caching, and round-robin load balancing. Also set up vitest and create unit tests for all new gateway modules.

Purpose: Transform the gateway from a simple proxy into a resilient, cost-optimizing API gateway that handles provider failures gracefully and reduces costs via smart routing and caching.
Output: 6 new/modified gateway files, vitest config, 4 test files, schema additions for smart routing and caching columns.
</objective>

<execution_context>
@/Users/hzmhezhiming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hzmhezhiming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-advanced-gateway-launch/05-CONTEXT.md
@.planning/phases/05-advanced-gateway-launch/05-RESEARCH.md
@.planning/phases/05-advanced-gateway-launch/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Vitest setup, schema additions, Redis client</name>
  <files>
    package.json,
    vitest.config.ts,
    src/lib/server/db/schema.ts,
    src/lib/server/redis.ts
  </files>
  <read_first>
    src/lib/server/db/schema.ts,
    src/lib/server/gateway/rate-limit.ts,
    package.json,
    svelte.config.js
  </read_first>
  <behavior>
    No TDD tests for this sub-task — it is configuration and schema work.
  </behavior>
  <action>
    1. Install dependencies: `npm install ioredis` and `npm install -D vitest @vitest/coverage-v8`

    2. Create `vitest.config.ts` at project root:
       - import { defineConfig } from 'vitest/config'
       - resolve alias: `$lib` -> `src/lib` (match SvelteKit paths)
       - test.include: `['src/**/*.test.ts']`

    3. Add to `src/lib/server/db/schema.ts` in the `appApiKeys` table definition:
       - `smartRouting: boolean('smart_routing').notNull().default(false)`
       Add to `appOrganizations` table definition:
       - `smartRoutingCheapModel: text('smart_routing_cheap_model')`
       - `smartRoutingExpensiveModel: text('smart_routing_expensive_model')`
       - `cacheTtlSeconds: integer('cache_ttl_seconds').notNull().default(3600)`

    4. Create `src/lib/server/redis.ts`:
       - Import Redis from 'ioredis' and env from '$env/dynamic/private'
       - `let redis: Redis | null = null`
       - Export `getRedis(): Redis | null` — returns null if `env.REDIS_URL` not set
       - On first call with REDIS_URL: create `new Redis(env.REDIS_URL, { maxRetriesPerRequest: 3, lazyConnect: true, retryStrategy(times) { return Math.min(times * 200, 2000); } })`
       - Attach `redis.on('error', () => {})` to prevent unhandled rejections
       - Return the Redis instance
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 || echo "No tests yet — config verified by import"</automated>
  </verify>
  <done>Vitest configured with SvelteKit path aliases. Schema has smartRouting, smartRoutingCheapModel, smartRoutingExpensiveModel, cacheTtlSeconds columns. Redis lazy singleton created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Gateway modules (load-balancer, routing, cache) with unit tests</name>
  <files>
    src/lib/server/gateway/load-balancer.ts,
    src/lib/server/gateway/load-balancer.test.ts,
    src/lib/server/gateway/routing.ts,
    src/lib/server/gateway/routing.test.ts,
    src/lib/server/gateway/cache.ts,
    src/lib/server/gateway/cache.test.ts
  </files>
  <read_first>
    src/lib/server/gateway/rate-limit.ts,
    src/lib/server/gateway/auth.ts,
    src/lib/server/gateway/proxy.ts,
    src/lib/server/redis.ts
  </read_first>
  <behavior>
    load-balancer.test.ts:
    - selectKeyRoundRobin with 3 keys returns them starting from rotation index
    - Consecutive calls rotate through keys (call 1 starts at index 0, call 2 at index 1, etc.)
    - Single key returns that key
    - Empty keys array returns empty array

    routing.test.ts:
    - estimateTokenCount returns Math.ceil(totalChars / 4) for messages array
    - estimateTokenCount handles messages with no content field (returns 0)
    - selectModelTier returns cheapModel when tokens < 500
    - selectModelTier returns expensiveModel when tokens >= 500
    - selectModelTier returns expensiveModel at exactly 500 tokens

    cache.test.ts:
    - generateCacheKey produces format "cache:{orgId}:{sha256hex}"
    - generateCacheKey produces same hash for messages differing only in whitespace
    - generateCacheKey produces different hash for different models
    - getCachedResponse returns null when Redis not available (getRedis returns null)
    - setCachedResponse is no-op when Redis not available
  </behavior>
  <action>
    1. Create `src/lib/server/gateway/load-balancer.ts`:
       - In-memory `rotationCounters = new Map<string, number>()`
       - Export `selectKeyRoundRobin(keys: T[], orgId: string, provider: string): T[]`
       - Counter key: `${orgId}:${provider}`
       - Get current counter (default 0), increment, compute `startIdx = current % keys.length`
       - Return reordered array: `[...keys.slice(startIdx), ...keys.slice(0, startIdx)]`
       - Handle empty keys array by returning []

    2. Create `src/lib/server/gateway/routing.ts`:
       - Export `estimateTokenCount(messages: Array<{ content?: string }>): number`
         - Sum all string content lengths, return `Math.ceil(totalChars / 4)`
       - Export `selectModelTier(estimatedTokens: number, cheapModel: string, expensiveModel: string, threshold = 500): string`
         - Return cheapModel if estimatedTokens < threshold, else expensiveModel

    3. Create `src/lib/server/gateway/cache.ts`:
       - Import `createHash` from 'crypto', `getRedis` from '$lib/server/redis'
       - Export `generateCacheKey(orgId: string, model: string, messages: unknown[]): string`
         - Normalize: `JSON.stringify(messages).replace(/\s+/g, ' ').trim()`
         - Hash: `createHash('sha256').update(model + ':' + normalized).digest('hex')`
         - Return `cache:${orgId}:${hash}`
       - Export `async getCachedResponse(key: string): Promise<string | null>`
         - Get redis via getRedis(), return null if no redis, otherwise `redis.get(key)`
         - Wrap in try/catch, return null on error
       - Export `async setCachedResponse(key: string, body: string, ttlSeconds: number): Promise<void>`
         - Get redis, return if no redis, otherwise `redis.setex(key, ttlSeconds, body)`
         - Wrap in try/catch, silently fail

    4. Write tests FIRST (RED), then implement each module (GREEN). In tests, mock `$lib/server/redis` for cache tests using vi.mock. For load-balancer and routing tests, no mocking needed (pure functions).

    5. Run `npx vitest run --reporter=verbose` — all tests must pass.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All new gateway modules (load-balancer, routing, cache) exist with passing unit tests. Pure functions tested directly, cache tests mock Redis.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate retry/fallback/routing/caching/load-balancing into proxy.ts, extend auth.ts, add unit + integration tests</name>
  <files>
    src/lib/server/gateway/proxy.ts,
    src/lib/server/gateway/auth.ts,
    src/lib/server/gateway/proxy.test.ts
  </files>
  <read_first>
    src/lib/server/gateway/proxy.ts,
    src/lib/server/gateway/auth.ts,
    src/lib/server/gateway/budget.ts,
    src/lib/server/gateway/usage.ts,
    src/lib/server/gateway/load-balancer.ts,
    src/lib/server/gateway/routing.ts,
    src/lib/server/gateway/cache.ts,
    src/lib/server/redis.ts,
    src/lib/server/db/schema.ts,
    src/routes/v1/chat/completions/+server.ts
  </read_first>
  <action>
    1. Extend `GatewayAuth` interface in `src/lib/server/gateway/auth.ts`:
       Add these fields:
       - `smartRouting: boolean`
       - In the `org` sub-object: `smartRoutingCheapModel: string | null`, `smartRoutingExpensiveModel: string | null`, `cacheTtlSeconds: number`
       Update the DB query that constructs GatewayAuth to select these new columns from the joined appApiKeys and appOrganizations tables.

    2. Add `fetchWithRetry` function in `src/lib/server/gateway/proxy.ts`:
       ```
       const RETRYABLE_STATUSES = new Set([429, 500, 503]);
       const MAX_RETRIES = 3;
       const BASE_DELAY_MS = 500;

       async function fetchWithRetry(url: string, init: RequestInit, retries = MAX_RETRIES): Promise<Response> {
         for (let attempt = 0; attempt <= retries; attempt++) {
           const response = await fetch(url, init);
           if (!RETRYABLE_STATUSES.has(response.status) || attempt === retries) return response;
           const delay = BASE_DELAY_MS * Math.pow(2, attempt);
           const jitter = delay * 0.25 * Math.random();
           await new Promise(resolve => setTimeout(resolve, delay + jitter));
         }
         throw new Error('Retry exhausted');
       }
       ```
       Export `fetchWithRetry` so it can be tested directly.

    3. Modify the `proxyToLiteLLM` function flow in proxy.ts. The new order is:
       a. Parse request body, extract model and messages
       b. **Smart routing** (before key selection): If `auth.smartRouting` is true AND `auth.org.smartRoutingCheapModel` and `auth.org.smartRoutingExpensiveModel` are both non-null:
          - Import and call `estimateTokenCount(body.messages)` from routing.ts
          - Import and call `selectModelTier(tokens, cheapModel, expensiveModel)` from routing.ts
          - Create a new body object with the substituted model (do NOT mutate original)
          - If no provider key matches the substituted model, fall back to the originally requested model
       c. **Cache check** (before provider call): If request is NOT streaming (`!body.stream`) and `getRedis()` returns non-null:
          - Import and call `generateCacheKey(orgId, effectiveModel, body.messages)` from cache.ts
          - Import and call `getCachedResponse(cacheKey)` from cache.ts
          - If cache hit: return `new Response(cached, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Cache': 'HIT' } })`
          - Skip budget check and usage logging for cache hits (they are free)
       d. **Budget check** (existing — only reached on cache miss)
       e. **Key selection with round-robin**: Replace the existing "first matching key" logic:
          - Find all active provider keys matching the effective model
          - Import and call `selectKeyRoundRobin(matchingKeys, orgId, provider)` from load-balancer.ts
          - This returns an ordered array of keys to try
       f. **Try each key with retry/fallback**: Loop through the ordered keys:
          - Decrypt the key
          - Call `fetchWithRetry(litellmUrl, requestInit)` instead of plain `fetch`
          - If response is OK or not retryable (not 429/500/503), use this response
          - If retryable and retries exhausted, move to next key in the array
          - Track `lastError` for each failed key
       g. **Cache set on success**: If response is OK AND request was non-streaming:
          - Read response body as text
          - Import and call `setCachedResponse(cacheKey, responseBody, auth.org.cacheTtlSeconds)` — fire-and-forget (no await needed in the critical path, but wrap in try/catch)
          - Return new Response with the body text and `'X-Cache': 'MISS'` header
       h. **All keys exhausted**: Return `lastError` response (the last provider error)
       i. For streaming responses: pass through SSE as before (no caching, but retry/fallback still applies per key attempt)

    4. Create `src/lib/server/gateway/proxy.test.ts` with unit AND integration tests:

       **Unit tests (fetchWithRetry):**
       - fetchWithRetry retries on 429 up to 3 times then returns last response
       - fetchWithRetry returns immediately on 200
       - fetchWithRetry returns immediately on 400 (non-retryable)
       - Mock fetch globally for these tests using vi.fn()

       **Integration tests (cache hit path + smart routing):**
       - Cache hit integration: mock getCachedResponse to return a JSON string, call proxyToLiteLLM (or the relevant exported function), assert response has X-Cache: HIT header and body matches cached content, assert fetch was NOT called (provider was skipped)
       - Smart routing substitution integration: mock estimateTokenCount to return 100 (below 500 threshold), set auth.smartRouting=true with cheapModel="gpt-4o-mini" and expensiveModel="gpt-4o", call proxy with model="gpt-4o", assert the fetch call was made with body containing model="gpt-4o-mini" (the cheap substitution)
       - For integration tests, mock the relevant gateway module imports (cache, routing, load-balancer) using vi.mock

    5. Run `npx vitest run --reporter=verbose` — all tests pass.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchWithRetry" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "selectKeyRoundRobin" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "getCachedResponse" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "selectModelTier" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "X-Cache" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "smartRouting" src/lib/server/gateway/auth.ts returns 0
    - grep -q "smartRoutingCheapModel" src/lib/server/gateway/auth.ts returns 0
    - grep -q "cacheTtlSeconds" src/lib/server/gateway/auth.ts returns 0
    - grep -q "RETRYABLE_STATUSES" src/lib/server/gateway/proxy.ts returns 0
    - grep -q "X-Cache.*HIT" src/lib/server/gateway/proxy.test.ts returns 0
    - grep -q "gpt-4o-mini" src/lib/server/gateway/proxy.test.ts returns 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Gateway proxy has retry with exponential backoff + jitter, fallback across multiple keys, smart routing model substitution, Redis cache check/set, round-robin key selection, and X-Cache headers. Auth includes smartRouting fields. Unit tests for fetchWithRetry and integration tests for cache hit path and smart routing substitution all pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all gateway tests
- `grep -rn "fetchWithRetry\|selectKeyRoundRobin\|getCachedResponse\|selectModelTier" src/lib/server/gateway/` shows all integrations
- `npm run build` succeeds (no type errors)
</verification>

<success_criteria>
- Gateway retries on 429/500/503 with exponential backoff (3 attempts, 500ms base)
- Gateway falls back to next provider key after retries exhausted
- Smart routing substitutes model based on token estimation threshold (500)
- Redis caching stores and retrieves non-streaming responses with configurable TTL
- Cache hits return X-Cache: HIT, misses return X-Cache: MISS
- Round-robin distributes across provider keys for same model
- All unit tests pass
- Integration tests verify cache hit path and smart routing substitution
- App builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-gateway-launch/05-01-SUMMARY.md`
</output>
